//File Folder structure in our app
//We have a feature folder in our app and inside that we will have all the featues(folder)
//Inside each feature(for eg landing) folder we will have we will have a screens folder inside it 
//and inside that screens folder we will have the scaffold part related to the specific feature

// Let's look at each feature independently :-

//Auth :-
//Inside the auth folder we have folders like screens,repository and controller
//Inside the screens folder it will contain the UI For the Auth Feature 
//Inside the repository folder it will contain all the firebase Functions related to auth feature
//Inside the controller folder  it will contain functions which will take the desired data fom the login Screen input fields and do some formatting
//if required and then send it to the repository folder to do the desired functions 